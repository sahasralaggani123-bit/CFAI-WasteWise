import React, { useState } from 'react';
import { Alert, ScrollView, StyleSheet, Text, TextInput, TouchableOpacity } from 'react-native';
import { api } from '../services/api';
import { COLORS } from '../utils/constants';
import { ScreenHeader } from './shared';
export default function RegisterScreen({ navigation }: any) { const [name,setName]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const submit=async()=>{ if(!name||!email||!password) return Alert.alert('Missing fields'); const r=await api.register(name,email,password); if(r.success) navigation.navigate('Login');}; return <ScrollView style={styles.container}><ScreenHeader title="Create Account" subtitle="Join WasteWise" back navigation={navigation}/><TextInput placeholder="Full Name" placeholderTextColor={COLORS.textMuted} style={styles.input} value={name} onChangeText={setName}/><TextInput placeholder="Email Address" placeholderTextColor={COLORS.textMuted} style={styles.input} value={email} onChangeText={setEmail}/><TextInput placeholder="Password" placeholderTextColor={COLORS.textMuted} style={styles.input} value={password} onChangeText={setPassword} secureTextEntry/><TouchableOpacity style={styles.btn} onPress={submit}><Text style={styles.btnText}>Create Account</Text></TouchableOpacity></ScrollView>; }
const styles=StyleSheet.create({container:{flex:1,backgroundColor:COLORS.background},input:{height:48,marginHorizontal:22,marginTop:16,borderWidth:1,borderColor:COLORS.border,borderRadius:10,paddingHorizontal:12,color:COLORS.text,fontWeight:'700'},btn:{margin:22,height:48,borderRadius:10,alignItems:'center',justifyContent:'center',backgroundColor:COLORS.green},btnText:{color:'#fff',fontWeight:'900'}});
